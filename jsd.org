#+begin_src emacs-lisp

;; frame setup
(setq initial-frame-alist '( (right . 0) (top . 0) (height . 64) (width . 102) ))
(setq default-frame-alist '( (right . 0) (top . 0) (height . 64) (width . 102) ))

;; default packages to load
(starter-kit-load "latex")

#+end_src
